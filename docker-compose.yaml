version: '3.8'
services:
  pumpfun-bot:
    build: .
    container_name: pumpfun-bot
    volumes:
      - ./coins.csv:/app/coins.csv
      - ./failed_accounts.csv:/app/failed_accounts.csv
    # Optionally, add environment variables:
    # environment:
    #   - NODE_ENV=production